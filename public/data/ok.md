🏷️ Available Tags ("tags": [...])
Use these keywords to display feature icons on the card:

freemium / keysystem — Displays a yellow key icon (use for tools with keys).
serverside — Displays a green server icon.
kernel — Displays a yellow microchip icon (Kernel-level warning).
decompiler — Displays a code icon.
multi-instance — Displays a multi-layer icon.
raknet — Displays a network icon.
insecure — Hides the card unless "Insecure Mode" is enabled in filters.
🛡️ Available Badges ("badges": [...])
Use these to add specific status indicators to the top-right of a card:

verified — Adds a green checkmark.
trending — Adds a trending arrow.
warning — Adds a yellow warning triangle.
warningred — Adds a red high-risk warning triangle.
💻 Supported Platforms ("platforms": [...])
windows
macos
android
ios
🔧 Tool Types ("type": "...")
internal — Labeled as Executor in the UI.
external — Labeled as Cheat Menu in the UI.
serverside — Labeled as Server-side in the UI.
💰 Pricing Slugs (for prices.json)
You can use any tool folder name (slug) in the prices.json logic to define:

freeProducts — Add the name to this list to show the "FREE" label.
offers — Define daily/monthly price points for paid tools.

open-source — Displays a GitHub icon.
ai — Displays a robot icon.
high-performance — Displays a bolt icon.
custom-ui — Displays a paint brush icon.
scripthub — Displays a book icon.
lite — Displays a leaf icon.